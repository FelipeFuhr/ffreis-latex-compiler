name: Docker Build

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - 'cmd/**'
      - 'internal/**'
      - 'containers/**'
      - 'go.mod'
      - 'go.sum'
      - '.github/workflows/docker-build.yml'
  push:
    branches: [main, master]
    paths:
      - 'cmd/**'
      - 'internal/**'
      - 'containers/**'
      - 'go.mod'
      - 'go.sum'
      - '.github/workflows/docker-build.yml'

permissions:
  contents: read
  packages: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

  cancel-in-progress: true

jobs:
  docker:
    permissions:
      contents: read
      packages: write
    uses: FelipeFuhr/ffreis-workflows-container/.github/workflows/container-build.yml@f17453949fd409a1e3a5d90fd4f2d0313c1b6088 # untagged; main @ 2026-07-21
    with:
      image: "ffreis-latex-compiler"
      containerfile: "containers/Dockerfile.cli"
      context: "."
      skip-if-missing: true
      push: false
    secrets: inherit
